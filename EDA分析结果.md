根据你现在 2.1～2.6 的 EDA，已经可以把问题压缩成几条非常清楚的主线了。

最值得先记住的是：**EDA 不是为了“找到一个模型名”，而是为了找到模型必须解决的数据困难。**

---

## 1. 根据 EDA，现在发现了哪些问题？

### 问题 A：用户历史很长，但真实时间尺度差异很大

2.1 发现用户序列整体很长，中位数接近 95；2.2 又发现相邻行为的真实时间间隔差异很大，有的相隔分钟，有的相隔数天。

所以问题不是简单的：

> “用户有很多历史行为。”

而是：

$$
\boxed{\text{历史顺序很重要，但序列位置并不能完全代表真实时间距离}}
$$

这意味着模型可能需要同时利用：

- 行为顺序；
- 行为类型；
- time gap / recency。

这是一个**用户侧建模问题**。

---

### 问题 B：广告侧存在非常严重的长尾

2.3 发现：

- item 历史交互次数中位数只有 2；
- 约 73% 的 item 历史交互不超过 5 次；
- Top 1% item 却贡献接近一半历史行为。

所以真正的问题不是“数据长尾”这四个字，而是：

$$
\boxed{\text{大量广告的 ID 表示缺乏足够历史交互来学习}}
$$

也就是说：

```text
Head item
历史数据很多
→ ID Embedding 相对容易学

Tail item
只有 1~2 条历史
→ ID Embedding 学习信号很弱
```

同时总体指标还容易被热门 item 主导，因此以后必须做：

$$
\text{Head / Mid / Tail 分组评估}
$$

---

### 问题 C：22.57% 的候选广告完全没有历史 ID 信息

这比 tail 更严重。

Tail 至少还有：

$$
1\sim2\text{ 条交互}
$$

但 unseen candidate 是：

$$
\boxed{0\text{ 条历史交互}}
$$

所以纯 ID 模型天然遇到困难：

> 一个训练阶段根本没见过的广告，怎么靠 ID embedding 表示？

这是目前非常重要的一个问题。

---

### 问题 D：但这些弱历史 / 无历史广告恰好有丰富的非 ID 信息

这是 2.4、2.5、2.6 最有价值的联合发现。

对于 history-unseen candidate：

- 绝大多数结构化侧特征都存在；
- 约 97.2% 有有效多模态向量。

对于 tail item：

- 多模态覆盖率也约 97.1%。

所以出现了一个非常漂亮的互补结构：

$$
\boxed{
\text{历史 ID 信号弱甚至不存在}
\quad
\text{但 side / multimodal 信息仍然丰富}
}
$$

于是自然产生一个待验证假设：

$$
H_1:
\text{非 ID 信息能否帮助 Tail / Unseen item 获得更好的表示？}
$$

这个问题不是我们提前想做“多模态推荐”以后硬找出来的，而是 EDA 自己推出来的。

---

### 问题 E：侧信息自身是异构的

2.6 又发现：

- 有单值类别特征；
- 有 multi-value list；
- 有大量缺失字段；
- cardinality 从个位数一直到 200 多万。

所以：

$$
\boxed{\text{侧信息不能用一种统一粗暴方式编码}}
$$

这更多属于**特征工程问题**，不是项目最核心的业务矛盾。

---

## 2. 现在最值得做的是什么？

如果按照现在已有证据排序，我会这样排：

| 优先级 | 问题 | 原因 |
|---|---|---|
| **1** | Tail / Unseen item 表示问题 | EDA 证据最完整，而且直接对应 candidate |
| **2** | 时间感知用户兴趣 | 数据中确实存在明显时间异质性 |
| **3** | 行为类型建模 | 曝光和点击语义不同，属于自然增强 |
| 必须做但非创新 | Popularity + Head/Tail 分组评估 | 保证实验可信 |
| 工程处理 | 多模态/侧特征缺失 | 缺失存在，但没集中在关键弱 ID 群体 |

所以目前最值得形成项目主线的是：

$$
\boxed{
\text{如何在广告历史交互从丰富 → 稀疏 → 完全不存在时，}
\newline
\text{利用侧信息与多模态获得可泛化的广告表示？}
}
$$

你甚至可以把广告看成一个连续谱：

```text
Head
历史 ID 信号丰富
        ↓
Mid
        ↓
Tail
历史 ID 信号很弱
        ↓
Unseen
历史 ID 信号完全不存在
```

然后研究：

> 随着 ID 信号变弱，side/mm 的收益是否越来越明显？

这个实验逻辑非常漂亮。

---

## 3. 但这里还有一个必须验证的问题

现在我们知道：

$$
22.57\%
$$

的 **candidate** 是 history-unseen。

但不知道真实点击目标中：

$$
P(y_{\text{target}}\text{ is unseen})
$$

到底有多少。

这非常关键。

假如 validation 中真实点击目标只有：

$$
0.5\%
$$

属于 unseen，那么 unseen 虽然在 candidate 里很多，却不一定是最主要的性能瓶颈。

反过来，如果：

$$
15\%\sim20\%
$$

真实目标都是训练历史未见 item，那它就是一个非常核心的问题。

所以在阶段 4 做时间切分时，我认为应该明确统计：

$$
\boxed{
\text{validation target 的 Head/Mid/Tail/Unseen 分布}
}
$$

这会决定“弱 ID 泛化”到底只是有趣支线，还是项目主线。

因此现在最准确的说法是：

> **Tail / Unseen + side/mm 是当前最有潜力的主问题，但需要 validation label 分布进一步确认其业务重要性。**