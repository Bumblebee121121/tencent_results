最稳妥的方法是：**运行命令时直接把终端输出重定向到日志文件**。这样即使关闭 VSCode 或终端，结果也不会丢失。

## 1. 在项目根目录创建日志文件夹

在 VSCode 的 CMD 终端运行：

```cmd
mkdir logs
```

项目结构会变成：

```text
TECENT/
├── data/
├── logs/
├── inspect_data.py
└── ...
```

## 2. 保存 Python 程序的运行结果

例如运行 `inspect_data.py`：

```cmd
python inspect_data.py > logs\inspect_data.log 2>&1
```

含义是：

```text
>       将正常输出写入文件
2>&1    将报错信息也写入同一个文件
```

运行结束后，打开：

```text
logs/inspect_data.log
```

就可以查看完整输出。

也可以在终端中直接打开：

```cmd
code logs\inspect_data.log
```

## 3. 覆盖和追加的区别

每次重新运行并覆盖旧日志：

```cmd
python inspect_data.py > logs\inspect_data.log 2>&1
```

保留旧内容，在文件末尾继续追加：

```cmd
python inspect_data.py >> logs\inspect_data.log 2>&1
```

其中：

```text
>     覆盖旧文件
>>    追加到旧文件末尾
```

对于不同实验，建议使用不同文件名：

```cmd
python inspect_data.py > logs\inspect_data.log 2>&1
python verify_download.py > logs\verify_download.log 2>&1
python train.py > logs\sasrec_train.log 2>&1
```

## 4. 同时在终端观看并保存

CMD 本身没有很好用的内置 `tee` 命令。最方便的方法是把 VSCode 终端切换为 PowerShell。

点击终端右上角的下拉箭头，选择：

```text
PowerShell
```

然后运行：

```powershell
python inspect_data.py 2>&1 | Tee-Object -FilePath logs\inspect_data.log
```

这样可以同时：

* 在终端实时看到输出；
* 将相同内容保存到 `logs\inspect_data.log`。

追加模式使用：

```powershell
python inspect_data.py 2>&1 | Tee-Object -FilePath logs\inspect_data.log -Append
```

这是后续训练模型时最推荐的方式。

## 5. 让 Python 输出立即显示

训练程序有时会因为缓冲，导致日志延迟出现。可以增加 `-u`：

### CMD

```cmd
python -u inspect_data.py > logs\inspect_data.log 2>&1
```

### PowerShell

```powershell
python -u inspect_data.py 2>&1 | Tee-Object -FilePath logs\inspect_data.log
```

`-u` 表示尽量实时输出，后续训练 SASRec 时很有用。

## 6. 推荐你以后统一这样运行

对于短小的数据检查程序：

```cmd
python inspect_data.py > logs\inspect_data.log 2>&1
python -X utf8 inspect_data.py > logs\inspect_data.log 2>&1
```
## python -X utf8 inspect_data.py > logs\inspect_data.log 2>&1

对于训练时间较长、需要边看边保存的程序，使用 PowerShell：

```powershell
python -u train.py 2>&1 | Tee-Object -FilePath logs\train.log
```

另外，在 `.gitignore` 中加入：

```gitignore
logs/
```D:\AI-file\tecent\.vscode\settings.json

避免把大量本地运行日志提交到 Git。

你现在运行数据检查脚本时，可以直接使用：

```cmd
python inspect_data.py > logs\inspect_data.log 2>&1
```

运行结束后，把 `logs/inspect_data.log` 上传给我即可。

# 后续更新结果
每完成一个 EDA 阶段：
git add . #选择本次要保存的文件 不上传到GitHub
git commit -m "update stage2 results" #创建本地版本 不上传到GitHub
git push #上传到GitHub
然后把仓库链接和具体文件路径发给网页版 GPT，例如：
请读取该仓库中的：
artifacts/eda/metrics/stage2_1_sequence_behavior.json
artifacts/eda/tables/
logs/stage2_1_sequence_behavior.log

结合 PNG 图分析阶段 2.1 的主要数据现象和可检验建模假设。
