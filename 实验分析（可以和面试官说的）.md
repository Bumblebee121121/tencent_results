# 我应该关注每个阶段用了什么方法？发现了什么问题？解决措施是什么？即可
# 阶段
# 数据理解与数据预处理
数据来自腾讯官方，但我没有直接套官方 Baseline。我先检查了数据 Schema 和跨表关联，发现历史序列与广告属性使用连续 RID，而候选表和多模态表使用匿名 OID，其中多模态 ID 还是字符串类型，因此我通过官方 indexer 完成统一映射。另外，原始曝光和点击编码为 0、1，而模型词表映射为 1、2，把 0 留给 Padding。完成这些数据契约确认后，我才进行 EDA 和模型训练。
# 阶段 1 总结：数据理解与数据预处理

## 先用一句话理解阶段 1

阶段 1 不是在“清洗一份很脏的数据”，也不是在做模型，而是在确认：

[
\boxed{
\text{数据是什么}
+
\text{各张表如何关联}
+
\text{是否可以被正确、稳定地用于后续实验}
}
]

对这个官方数据集来说，阶段 1 更接近于：

> **读懂数据说明书，并验证我们对数据的理解与本地数据实际结构一致。**

---

# 版本一：详细版

## 1. 阶段 1 的背景与目标

我们当前要做的是一个广告序列推荐项目：根据用户已有的曝光、点击行为历史，从候选广告池中预测用户下一次可能点击的广告。

在正式进行 EDA、特征工程和模型训练之前，需要先解决几个基础问题：

* 数据由哪些表组成？
* 每张表的字段和数据类型是什么？
* 用户行为序列如何表示？
* 历史广告、候选广告和多模态广告是否使用相同 ID？
* 用户、广告及多模态信息能否正确关联？
* 是否存在会直接破坏训练结果的异常？

因此，阶段 1 的核心不是提升模型指标，而是建立可靠的**数据契约**：

[
\boxed{
\text{字段定义}
+
\text{编码规则}
+
\text{跨表关联规则}
+
\text{已知缺失与异常处理原则}
}
]

---

## 2. 阶段 1.1：检查文件、Schema 和样例

### 所属工作

```text
阶段 1：数据理解与数据预处理
└── 1.1 数据结构检查
```

### 我做了什么

我使用 PyArrow 读取了以下数据表的 Schema 和少量样例：

```text
seq
item_feat
user_feat
candidate
mm_emb_81_32
indexer.pkl
```

只读取字段结构和前两行，没有把全量数据一次性加载到内存。

### 为什么这样做

官方数据由多张 Parquet 表组成，而且部分字段是嵌套结构。如果只看文件名，无法确定：

* 哪些字段是整数；
* 哪些字段是列表；
* 哪些字段存在缺失；
* 候选表是否与历史广告表具有相同结构；
* 多模态向量的 ID 和维度是什么。

如果没有理解 Schema 就直接建模，可能出现“代码能够运行，但字段含义和连接方式错误”的情况。

### 发现了什么

#### 用户行为序列表 `seq`

每个用户对应一个变长序列：

```text
user_id
seq = [
    {
        item_id,
        action_type,
        timestamp
    },
    ...
]
```

数学上可以写为：

[
S_u=
\left[
(i_1,a_1,t_1),
(i_2,a_2,t_2),
\ldots,
(i_T,a_T,t_T)
\right]
]

说明该数据天然适合行为序列建模。

#### 广告特征表 `item_feat`

包含：

```text
item_id
100, 101, 102, 112, ..., 122
```

这些是匿名广告特征，且部分字段存在空值，例如字段 `115`。

#### 用户特征表 `user_feat`

同时存在两种特征：

```text
单值特征：103、104、105、109
列表特征：106、107、108、110
```

这意味着后续不能把所有用户特征都按普通单值类别特征处理。列表字段可能需要 Embedding Pooling。

#### 候选表 `candidate`

每个匿名特征不是简单整数，而是嵌套结构：

```text
{
    cold_start,
    feature_value
}
```

其中 `feature_value` 为字符串，和历史广告表中的整数类型并不完全一致。

#### 多模态表 `mm_emb_81_32`

包含：

```text
anonymous_cid
emb
```

其中 `anonymous_cid` 是字符串，`emb` 是 32 维向量。

### 对后续的影响

这一步告诉我们：

* `seq` 是序列推荐的核心表；
* 用户列表特征需要特殊编码；
* 缺失值不能直接当作正常类别；
* 候选表不能直接与历史广告表按同名字段无脑连接；
* 多模态 ID 需要转换和映射。

---

## 3. 阶段 1.2：检查 `indexer.pkl` 的编码体系

### 所属工作

```text
阶段 1：数据理解与数据预处理
└── 1.2 ID 和特征编码体系分析
```

### 我做了什么

我加载并查看了 `indexer.pkl` 的顶层结构，但没有打印数百万条完整映射。

发现其结构为：

```python
indexer = {
    "f": ...,  # 匿名特征值映射
    "i": ...,  # 广告 ID 映射
    "u": ...,  # 用户 ID 映射
    "a": ...,  # 行为类型映射
}
```

其规模为：

| 映射         |        数量 |
| ---------- | --------: |
| 物品映射 `i`   | 4,783,154 |
| 用户映射 `u`   | 1,001,845 |
| 行为映射 `a`   |         2 |
| 特征命名空间 `f` |        22 |



### 为什么这样做

在推荐系统中，原始 ID 往往：

* 数值很大；
* 不连续；
* 可能是字符串；
* 不适合直接作为 Embedding 下标。

因此通常会重映射为连续整数：

[
\text{Original ID}
\longrightarrow
\text{Remapped ID}
]

连续 RID 便于建立 Embedding 表，也可以把 `0` 留给 Padding。

### 发现了什么

* `indexer["i"]` 是原始广告 OID 到连续 RID 的映射；
* `indexer["u"]` 是原始用户字符串到用户 RID 的映射；
* `indexer["f"]` 保存各匿名字段的类别映射；
* `indexer["a"]` 保存行为类型编码。

这一步让我们知道，数据里不止存在一种 ID 表示。

---

## 4. 阶段 1.3：验证真实 OID→RID 映射

### 所属工作

```text
阶段 1：数据理解与数据预处理
└── 1.3 跨表 ID 映射样例验证
```

### 我做了什么

我从 `candidate` 和多模态表中读取实际 OID，并通过：

```python
indexer["i"]
```

查找对应 RID。

同时检查了行为编码和特征词表。

### 为什么这样做

仅知道“存在映射字典”还不够，还需要确定：

* 映射方向；
* 候选广告是否能够映射；
* 多模态字符串 ID 是否需要先转整数；
* `seq` 和 `item_feat` 是否已经使用映射后的 RID。

### 发现了什么

候选广告样例：

[
20000006363
\longrightarrow
15543
]

多模态广告样例：

[
\text{"20000000029"}
\longrightarrow
20000000029
\longrightarrow
2014425
]

行为类型编码为：

[
0\rightarrow1,\qquad 1\rightarrow2
]

因此确定：

| 数据表                    | 使用的广告 ID   |
| ---------------------- | ---------- |
| `seq.item_id`          | RID        |
| `item_feat.item_id`    | RID        |
| `candidate.item_id`    | OID        |
| `mm_emb.anonymous_cid` | 字符串形式的 OID |

跨表关联必须经过：

[
\boxed{
\text{OID}
\overset{\text{indexer["i"]}}{\longrightarrow}
\text{RID}
}
]



### 发现的关键问题

如果直接按数字连接：

```text
seq/item_feat
和
candidate/mm_emb
```

会导致匹配失败或特征错位。

特征错位很危险，因为它通常不会导致程序报错，却可能把广告 A 的多模态向量分配给广告 B，使模型训练结果失去意义。

---

## 5. 阶段 1.4A：核心行为数据健康检查

### 所属工作

```text
阶段 1：数据理解与数据预处理
└── 1.4A seq、user_feat、item_feat 全量一致性检查
```

### 我做了什么

我扫描了全量：

```text
seq
user_feat
item_feat
```

检查：

* 主键是否为空或重复；
* 用户序列是否为空；
* 广告 ID 是否非法；
* 行为类型是否非法；
* 时间戳是否逆序；
* 用户特征和广告特征跨表覆盖率。

### 为什么这样做

前面的检查只验证了少量样例。本步需要确认：

> 样例中成立的关联规则，在全量 100 万用户和 9000 多万行为上是否仍然成立。

### 发现了什么

数据规模：

| 内容    |         数量 |
| ----- | ---------: |
| 用户数   |  1,001,845 |
| 历史广告数 |  4,783,154 |
| 总行为数  | 90,223,574 |

核心质量结果：

* `user_feat` 主键无缺失、无重复；
* `item_feat` 主键无缺失、无重复；
* `seq` 用户无缺失、无重复；
* 空序列数为 0；
* 广告 ID 无缺失、无非法值；
* 时间戳无缺失；
* 所有用户序列均不存在时间逆序；
* `seq` 用户在 `user_feat` 中覆盖率为 100%；
* `seq` 广告在 `item_feat` 中唯一广告覆盖率和行为级覆盖率均为 100%。

因此：

[
\boxed{
\text{seq、user_feat 和 item_feat 三张核心表可以完整关联}
}
]



---

## 6. 阶段 1.4A 补充：分析 `action_type` 缺失

### 为什么需要补充检查

全量扫描发现：

```text
action_type 缺失：790,581 条
```

虽然非空行为全部是合法的 `0/1`，但我们不能直接假设空值表示曝光或点击。

于是进一步检查了缺失位置和用户分布。

### 发现了什么

* 缺失比例为 `0.876247%`；
* 涉及 344,817 名用户；
* 绝大多数缺失位于序列中间；
* 只有很少一部分是“恰好在序列最后一个位置缺失”；
* 有些用户存在多次缺失，最多达到几十次。

具体位置：

| 位置      |    缺失次数 |
| ------- | ------: |
| 序列第一个位置 |   1,546 |
| 序列中间    | 769,748 |
| 序列最后    |  19,287 |

因此可以排除：

> `action_type=None` 是统一的目标行为标记。



### 当前处理原则

不修改原始数据，并将其解释为：

```text
0    = 曝光
1    = 点击
None = 未知行为类型
```

后续：

* 不使用行为类型的模型仍保留这些事件；
* 使用行为 Embedding 时设置独立的“未知行为”编码；
* 不把缺失值强行填成曝光或点击；
* 行为监督相关任务中，对无法确定的标签进行屏蔽。

### 为什么不能直接删除

这些事件的：

```text
item_id
timestamp
```

仍然完整。

删除整条事件会改变用户真实行为序列，也可能破坏时序信息。

---

## 7. 阶段 1.4B：候选池与多模态一致性检查

### 所属工作

```text
阶段 1：数据理解与数据预处理
└── 1.4B candidate、mm_emb 与 indexer 一致性检查
```

### 我做了什么

全量检查：

* 候选广告主键和 `retrieval_id`；
* 候选 OID 到历史 RID 的映射率；
* 多模态 OID 是否有效和重复；
* 多模态向量维度；
* 是否存在 `NaN`、`Inf` 或全零向量；
* 历史广告多模态覆盖率。

### 候选池发现

候选广告共：

[
660000
]

并且：

* `item_id` 无缺失、无重复；
* `retrieval_id` 无缺失、无重复；
* `retrieval_id` 从 0 到 (N-1) 连续。

其中：

[
511029
]

个候选能够映射到历史 RID，

[
148971
]

个候选不能映射，映射率为：

[
77.43%
]

也就是说，大约：

[
22.57%
]

的候选广告没有出现在历史广告映射中。

### 应如何理解

这不属于数据损坏。

更谨慎的表述是：

> 候选池中包含一批历史 ID 空间中未出现的广告。

它们可能对应新广告或冷启动候选，但是否能正式定义为冷启动，还需要结合候选特征和后续 EDA 验证。

### 多模态数据发现

多模态记录共：

[
4742961
]

其中：

* OID 无缺失；
* OID 无重复；
* 所有非空向量均为 32 维；
* 没有 `NaN` 或 `Inf`；
* 没有全零向量；
* 有 9,678 条 Embedding 为空。

多模态 OID 映射到历史 RID 的比例为：

[
96.89%
]

历史广告的多模态 ID 覆盖率约为：

[
96.08%
]



### 对后续的影响

后续不能假设：

```text
每个广告都有多模态向量
```

更合理的做法是显式区分：

```text
mm_available = 1  # 多模态可用
mm_available = 0  # 多模态缺失
```

不能简单把缺失补成零后，当作真实的零向量处理。

---

# 8. 阶段 1 最终发现的问题

## 问题一：不同表使用不同的广告 ID 空间

```text
历史序列、广告特征：RID
候选表、多模态表：OID
```

解决方式：

[
\text{OID}
\overset{\text{indexer["i"]}}{\longrightarrow}
\text{RID}
]

这是后续所有跨表关联的基础。

---

## 问题二：`action_type` 存在少量缺失

缺失比例约为：

[
0.876%
]

并且不是统一集中在序列末尾。

处理原则：

* 不伪造为曝光或点击；
* 保留事件中的广告和时间信息；
* 行为模型中使用独立未知类别；
* 行为标签监督时屏蔽未知值。

---

## 问题三：部分候选广告没有历史 RID

约：

[
22.57%
]

的候选广告不在历史广告映射中。

这意味着纯 ID Embedding 模型无法直接为这些历史未见广告学习有效表示，也自然引出了后续问题：

> 是否需要利用广告匿名属性和多模态信息表示历史未见候选？

但当前还不能宣称多模态一定能解决，需要通过 EDA 和实验验证。

---

## 问题四：多模态覆盖较高，但并不完整

历史广告多模态 ID 覆盖率约为：

[
96.08%
]

同时还有 9,678 条空 Embedding。

因此后续多模态融合必须考虑：

* 模态缺失；
* 缺失指示变量；
* 缺失广告的默认表示；
* 分组评估有无多模态广告。

---

# 9. 阶段 1 没有做什么

为了避免把不同阶段混在一起，阶段 1 尚未进行：

* 没有分析序列长度分布；
* 没有分析点击和曝光比例的业务含义；
* 没有分析时间间隔；
* 没有分析广告长尾；
* 没有决定匿名特征的 Embedding 维度；
* 没有进行训练/验证/测试切分；
* 没有进行负采样；
* 没有训练任何推荐模型；
* 没有计算 Recall、NDCG 等指标。

这些分别属于：

```text
阶段 2：EDA 与问题发现
阶段 3：特征工程
阶段 4：样本构造和评估协议
阶段 5：模型训练
```

---

# 10. 阶段 1 的最终结论

核心数据质量良好：

* 文件和 Schema 可正常读取；
* 核心主键无缺失、无重复；
* 用户和广告核心表覆盖率为 100%；
* 序列时间顺序正确；
* OID/RID 映射规则已经确认；
* 候选池和多模态向量结构合法；
* 所有已发现的缺失和不覆盖现象均已量化，并有保守处理原则。

因此：

[
\boxed{
\text{数据具备进入阶段 2 EDA 的条件}
}
]

阶段 1 的价值不是“修复了腾讯官方数据”，而是确保：

[
\boxed{
\text{后续分析和模型使用的是正确关联、正确解释的数据}
}
]

---

# 11. 面试官可能追问的问题

## 问：官方数据为什么还要检查？

可以回答：

> 我并不是怀疑官方数据本身，而是验证本地文件、实际 Schema 和官方数据契约是否一致。尤其是这个数据集中历史序列使用 RID，而候选和多模态表使用 OID，如果没有先验证映射，后续很容易出现跨表匹配失败或多模态特征错位。

## 问：为什么不直接删除 `action_type` 为空的事件？

可以回答：

> 因为空值只出现在行为类型上，对应的广告 ID 和时间戳仍然完整。直接删除会破坏用户行为序列。我的处理是把它保留为一次广告交互，在不依赖行为类型的模型中正常使用；在行为感知模型里设置独立未知类别，在行为监督损失中屏蔽未知标签。

## 问：22.57% 的候选无法映射，是数据错误吗？

可以回答：

> 不是。候选池结构本身完整，无法映射只说明这些广告没有出现在历史物品映射空间中。它们可能是历史未见或冷启动候选，因此不能删除。后续需要通过候选特征和分组实验验证侧信息对这些广告的作用。

## 问：为什么多模态缺失不能直接补零？

可以回答：

> 因为零向量可能被模型理解为一种真实的数值模式，而不是“该模态不可用”。因此除了补默认值外，还要增加显式的模态可用标记，避免模型混淆真实表示和缺失状态。

---

# 版本二：面试口述简洁版

> 这个项目的第一阶段是数据理解和预处理。我没有直接运行官方 Baseline，而是先检查了各张 Parquet 表的 Schema、主键、行为序列和跨表关联。过程中发现历史序列和广告属性使用连续 RID，而候选广告及多模态表使用匿名 OID，所以我通过官方 `indexer.pkl` 统一了 OID 到 RID 的映射，避免侧信息错位。全量检查结果显示，约 100 万用户、478 万历史广告和 9022 万条行为的核心主键都没有重复或非法值，用户和广告特征覆盖率均为 100%，序列时间戳也没有逆序。同时我发现约 0.88% 的行为缺少 `action_type`，因此没有强行填成曝光或点击，而是保留为未知行为；候选池中约 22.57% 的广告没有历史 RID，多模态对历史广告的覆盖约为 96%，这为后续冷启动和缺失感知建模提供了问题来源。完成这些检查后，我确认数据可以可靠进入 EDA 和模型实验阶段。

## 更短的 20 秒版本

> 第一阶段我主要完成了数据结构理解和全量一致性检查，确认了用户、广告、行为序列及多模态表的关联规则。核心发现是历史表使用 RID，而候选和多模态表使用 OID，需要通过官方索引统一映射；另外存在少量未知行为、历史未见候选和多模态缺失。核心表主键、时间顺序及跨表覆盖都正常，因此数据具备进入 EDA 和建模的条件。

# 阶段2：EDA 与问题发现
# 2.1 用户序列长度与行为构成
我首先分析了用户序列长度和行为构成。TencentGR-1M 中用户历史序列整体较长，中位数达到 95，约 85% 的用户序列长度在 81 到 100 之间，并且在最大长度 100 处存在明显堆积，因此数据具有较强的序列建模基础。行为方面约 89% 为曝光、9.7% 为点击，存在明显行为类型不平衡，但用户层面并不严重缺少点击反馈，因为超过 94% 的用户至少有两次点击记录。因此后续我会验证显式行为类型建模和时间建模，而不是简单地把问题定义成正样本极度稀疏。
# 2.2 时间模式
在确认用户序列整体较长后，我进一步从用户级和事件级分析真实时间尺度。用户级上统计首尾行为的时间跨度，事件级上统计同一用户相邻行为的 timestamp 差，并用分位数和时间区间描述长尾分布。结果发现相邻行为的真实时间距离差异很大，因此序列位置并不能完全代表真实时间距离。基于这个现象，我提出模型可能需要显式利用 time-gap 或 recency 信息，至于采用哪一种序列模型，会结合后续长尾、冷启动和侧信息 EDA 结果一起决定。       
# 2.3 广告长尾
        
# 2.4 candidate seen/unseen + cold_start
        
# 2.5 多模态覆盖与缺失模式
        
# 2.6 匿名特征 profile
